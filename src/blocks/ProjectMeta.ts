import type { Block } from "payload";

export const ProjectMeta: Block = {
  slug: "project-meta",
  labels: {
    singular: "Barre méta (type · période · stack · github)",
    plural: "Barres méta",
  },
  fields: [],
};

export default ProjectMeta;
