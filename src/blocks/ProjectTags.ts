import type { Block } from "payload";

export const ProjectTags: Block = {
  slug: "project-tags",
  labels: {
    singular: "Tags (stack)",
    plural: "Tags (stack)",
  },
  fields: [],
};

export default ProjectTags;
